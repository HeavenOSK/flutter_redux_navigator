name: redux_navigator
description: A Redux middleware for Navigator of Flutter. You can use default Navigator related Action, and also add custom behaviors.
version: 0.2.0
author: Ryunosuke Watanabe <heavenosk@gmail.com>
homepage: https://github.com/HeavenOSK/redux_navigator
environment:
  sdk: ">=2.2.3 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  # todo: update to ^4.0.0
  redux: ^3.0.0
  injectable_middleware: ^0.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic_mono: ^1.4.0
flutter:
